// https://en.wikipedia.org/wiki/Bloom_filter

// inserisce un elemento nel bloom filter
// insert(x) -> void

// controlla se x è un elemento del bloom filter e risponde
// false se x sicuramente non è presente
// true se x può essere presente oppure no
// lookup(x) -> boolean

// 3 funzioni di hash che vengono mappate in un array di bit (0/1)